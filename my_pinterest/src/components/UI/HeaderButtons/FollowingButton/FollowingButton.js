import React from 'react';
import classes from './FollowingButton.css'

const followingButton = (props) => (
    <div className = {classes.FollowingButton}>
        Following
    </div>
);

export default followingButton;

